# 人工智能导论大作业小组分工方案

## 一、项目基本信息

本项目题目为**可解释的谣言检测**，目标是构建一个能够对英文社交媒体文本进行谣言分类，并输出自然语言判断依据的系统。

系统输入一段英文文本，输出：

- `label`：分类结果，`0` 表示非谣言，`1` 表示谣言
- `reason`：对分类结果的自然语言解释

项目计划采用由浅入深的技术路线：首先实现 `TF-IDF + Logistic Regression` baseline，验证数据读取、文本预处理、模型训练和预测流程；随后尝试 BERT/RoBERTa 等预训练 Transformer 模型提升分类性能；最后根据实验结果探索模型集成方法，进一步提高验证集准确率和稳定性。

## 二、总体技术路线

本项目整体分为四个部分推进：

1. **数据处理与 baseline 建模**：完成数据读取、文本清洗、特征提取和传统机器学习模型训练，建立可对比的基础模型。
2. **深度学习模型优化**：在 baseline 基础上尝试 BERT/RoBERTa 等预训练语言模型，并通过参数调整提升验证集效果。
3. **可解释性输出**：结合文本特征、关键词、来源表达和模型预测结果，为每条输入生成自然语言判断依据。
4. **系统整合与报告撰写**：统一训练、预测、解释和结果复现流程，整理实验结果，完成 README、报告和展示材料。

## 三、小组成员与分工安排

### 1. 钟睿（组长，Git：Rui2418）

#### 负责方向

钟睿作为组长，主要负责项目统筹、仓库维护、系统框架搭建、深度学习模型训练优化以及最终系统整合。

#### 具体任务

1. 创建并维护项目 GitHub 仓库，设计项目目录结构和代码组织方式。
2. 整理课程数据集，统一训练集、验证集、模型文件和输出文件路径。
3. 搭建项目基础框架，包括配置文件、主程序入口、训练脚本、预测脚本和解释模块接口。
4. 负责 BERT/RoBERTa 等预训练 Transformer 模型的训练和调参。
5. 配置并测试 CUDA 训练环境，在有 GPU 的情况下提高模型训练效率。
6. 根据实验结果探索模型集成方案，保存最佳验证集结果和复现脚本。
7. 负责最终系统整合，确保可以通过命令行完成单条文本预测和批量预测。
8. 统筹 README、项目报告和展示材料的整体结构，检查最终提交版本完整性。

#### 预期产出

- 项目基础框架和目录结构
- 统一配置文件与主入口程序
- BERT/RoBERTa 训练、预测和调参脚本
- 最终模型或集成方案复现脚本
- README 和最终项目整合版本

### 2. 陈依宁（Git：Alice）

#### 负责方向

陈依宁主要负责数据预处理、传统机器学习模型实验、baseline 改进和错误分析，为后续深度学习模型和集成模型提供对照实验基础。

#### 具体任务

1. 分析 `train.csv` 和 `val.csv` 的字段、标签分布、缺失值和文本基本特征。
2. 设计并比较不同英文文本预处理策略，例如大小写处理、URL 清理、特殊符号处理等。
3. 实现 `TF-IDF + Logistic Regression` baseline，并记录验证集表现。
4. 对比多种传统分类器，如 Logistic Regression、Linear SVM、Naive Bayes 等。
5. 调整 TF-IDF 参数，包括 n-gram 范围、词频阈值、最大特征数等。
6. 进行预处理策略、特征参数和分类器组合的联合搜索，筛选传统模型中的较优方案。
7. 保存传统模型预测结果，为模型对比和后续集成提供支持。
8. 对误分类样例进行分析，总结模型容易出错的文本类型，为报告实验分析部分提供素材。

#### 预期产出

- 数据分析与预处理实验结果
- baseline 训练结果
- 传统分类器对比结果
- TF-IDF 参数调优记录
- 错误分析材料
- 可用于模型集成的传统模型预测文件

### 3. 许誉文

#### 负责方向

许誉文主要负责可解释性方案设计、案例整理、报告素材收集和展示支持，配合分类模型结果完成项目的解释输出和文档材料。

#### 具体任务

1. 查阅谣言检测、可解释文本分类和社交媒体文本分析相关资料，为报告背景和方法介绍提供素材。
2. 参与设计判断依据生成规则，从文本中的关键词、情绪表达、来源信息和不确定表达等角度生成解释。
3. 协助整理典型案例，包括输入文本、真实标签、预测结果和解释内容。
4. 对模型输出的 `reason` 进行人工检查，确保解释语句通顺、具体，并与分类结果保持一致。
5. 整理实验结果表格和案例展示内容，辅助完成报告中的实验结果与案例分析部分。
6. 检查 README 和报告中的运行命令、输出格式、项目结构说明是否清晰。
7. 参与最终展示材料制作，负责部分项目背景、可解释性设计和案例展示内容。

#### 预期产出

- 可解释性设计思路
- 典型案例整理材料
- 报告背景资料和案例分析素材
- 实验结果表格辅助整理
- README 与展示材料检查意见

## 四、协作规范

为保证项目顺利推进，小组计划采用以下协作方式：

1. 使用 GitHub 进行代码和文档协作，主要功能通过分支或 Pull Request 合并。
2. 每位成员负责相对独立的模块，完成后再由组长统一整合。
3. 提交信息尽量清晰说明修改内容，例如 `feat: add baseline model`、`docs: update report` 等。
4. 重要实验结果需要保存对应输出文件、参数设置和评价指标，方便后续复现和写入报告。
5. 代码提交前尽量本地运行，确保不会破坏主流程。
6. 报告和展示材料由三人共同补充，最后由组长统一格式和内容。

## 五、阶段安排

### 第一阶段：完成基础框架和 baseline

- 钟睿负责搭建项目结构、配置文件和主程序入口。
- 陈依宁负责完成数据分析、文本预处理和 baseline 模型训练。
- 许誉文负责查阅背景资料，初步设计解释模块思路。

### 第二阶段：模型优化和实验对比

- 钟睿负责尝试 BERT/RoBERTa 等预训练模型，并进行 CUDA 训练和参数调优。
- 陈依宁负责传统模型调参、分类器对比和联合搜索实验。
- 许誉文负责整理实验结果表格和初步案例分析。

### 第三阶段：解释模块和系统整合

- 钟睿负责统一预测接口，将分类模型和解释模块整合到系统中。
- 陈依宁负责提供误分类样例和模型对比分析。
- 许誉文负责完善解释文本、整理典型案例和报告中的可解释性部分。

### 第四阶段：最终提交和展示

- 钟睿负责检查仓库结构、README、复现命令和最终提交版本。
- 陈依宁负责补充实验对比、错误分析和模型效果说明。
- 许誉文负责补充背景介绍、案例展示和报告文字检查。

## 六、最终交付物

项目最终计划提交以下内容：

- 完整项目代码
- `README.md` 项目说明和运行指南
- 训练、预测和解释相关脚本
- 验证集预测结果与评价指标
- 可解释性案例材料
- 课程报告和展示材料
- 小组分工方案
